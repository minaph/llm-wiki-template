# TODO — 人間向けの要望窓口

このファイルは、人間がLLM Wikiへ明示的な要望を書き込み、その現在状態を短時間で確認するための入口である。機能追加、修正、調査、優先度変更、文書改善など、**人間が実施を望んでいること**をここで追跡する。

観察、感想、一般的な知識は、具体的な変更要求を含まない限りTODOへ入れない。再利用価値があれば `wiki/` または `runs/` へ記録する。一つの発言が観察と要望の両方を含む場合は、要望をTODOへ、知見をWikiまたはRunへ分けて扱う。

## 使い方

人間は `Open` に `- [ ] 要望` の形で自由に追記できる。参加者はユーザーの意図を保ったまま必要な補足をインデントして追加し、Taskへの形成先や阻害要因を明示する。

Taskを始める前にこのファイルをその都度読み直し、新しい要望、優先度変更、完了済みなのに未更新の項目がないか確認する。Taskを終える前にも再読し、今回の作業で状態が変わった項目を更新する。

TODO項目とTaskは同じものではない。一つの要望は既存Taskの一部、単発Task、複数Task、直接作業、保留、Task化しない判断のいずれにもなりうる。Taskが一回成功しても、人間の要望全体が満たされていなければTODOを完了にしない。

### 推奨する補足

必要な場合だけ、チェック項目の下に次を追加する。

- `状態:` `open / planned / in-progress / blocked` など現在状態。
- `実行先:` 関連する `tasks/*.md`、Issue、PR等への参照。
- `根拠:` 要望が記録されたIssue、会話、議事録等への参照。
- `完了条件:` チェックを付けるために満たすべき人間向けの結果。

状態語彙を増やすこと自体を目的にしない。Markdownチェックボックスを現在状態の主要表示とし、機械可読な別バックログを同時に正本化しない。

## Open

<!-- 人間はこの下へ自由に追記できる。例:
- [ ] 検索結果の待ち時間を短くしてほしい
  - 状態: planned
  - 実行先: [設計案の比較・検討](tasks/design-option-exploration.md)
  - 完了条件: 合意した実利用条件で目標応答時間を満たす
-->

## Closed

完了、取り下げ、または明示的に採用しないと判断した項目を移す。`[x]` は「要求が閉じた」ことを示し、必要なら `(done)`、`(declined)`、`(superseded)` など理由を短く添える。
