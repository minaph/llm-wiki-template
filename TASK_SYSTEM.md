# Taskの運用

Taskは、設計理解、要求との整合、未検証事項、選択肢の比較、コードと文書のずれを継続的に扱う作業である。運用ルールの整備は、それらの作業を進めやすくするために行う。具体的な問題がなければ主要な開発・設計・検証を優先する。

## 作業候補をTaskにする

作業中に問題や改善機会を見つけたら、必要に応じて [作業候補](tasks/proposals.md) へ記録する。目的、必要期間、繰り返す理由、引継ぎの必要性、終了条件から、次の扱いを選ぶ。

- **既存Taskの一部として行う**：目的・品質基準・停止条件が既存Taskと同じ。
- **単発のTaskにする**：一回または少数回で終わるが、後日または別の実行者へ引き継ぐ必要がある。
- **完了まで集中的に進めるTaskにする**：複数回の作業が必要で、明確な達成条件がある。
- **条件付きで実行するTaskにする**：障害再発、閾値超過、特定の移行などをきっかけに実施する。
- **常設のTaskにする**：独立した責任があり、繰り返すことで新しい価値が生まれる。
- **Taskにしない**：既存Taskで十分、根拠が弱い、費用に見合わない、まだ具体的な問いがない。

これらは存続形態や実施方法であり、`type` の値には使わない。個別Taskの本文に期間・頻度・終了条件として記す。

「問題の発見 → 作業候補の記録 → 扱いの判断 → 必要なら試行 → 実施・引継ぎ → 終了・再評価」を目安にする。候補数やTask数の増加を成果とみなさない。

## Taskに書く内容

Taskは、何を根拠に何を行い、どこまで進めればよいかを定める。[テンプレート](tasks/_TEMPLATE.md) を使い、複雑さとリスクに応じた詳しさで書く。

- **目的・判断対象**：何を理解、判断、検証、保守するか。
- **一次資料・証拠**：コード、Git履歴、要求、ログ、テスト、外部仕様等。
- **必要な行為**：読解、比較、静的解析、実行、測定、改稿等。
- **一回の作業の完了条件**：今回の範囲について、どの確認と記録まで終えるか。
- **品質基準**：根拠との一致、反例の検討、実行確認、文章の読み直し等。
- **存続形態・終了・停止・引継ぎ**：繰り返す理由、Task自体の終了条件、作業を止める理由、再開に必要な条件。
- **記録先**：結果・判断を書くWikiと、実行経緯を書くRun Note。

一回の作業の完了とTask自体の終了は分けて考える。具体的な終了・中断・再開の判断は個別Taskの本文に定め、共通の状態遷移は設けない。

## メタデータ

各Taskは、次の項目を空欄にせず記入する。

| 項目 | 意味 |
|---|---|
| `id` | 文書を参照するための重複しないID。既存の標準TaskのIDは維持する。 |
| `type` | 作業内容。標準値は `maintenance`（保守）、`review`（点検）、`exploration`（調査・比較）、`verification`（実行による検証）、`work`（その他）。必要なら内容を説明できる名称を追加する。 |
| `status` | 現在の扱い。標準Taskは `active`（選択可能）、`conditional`（条件成立時に選択）、導入時に使わないものは `paused`（休止）とする。他の値の意味や変更条件は個別Taskに書く。 |
| `priority` | 下記の `P0`〜`P4`。 |
| `trigger` | 実施を検討するきっかけ。随時なら `on-demand`。 |
| `required_capabilities` | 必要能力の空でないリスト。例：`[code-read, doc-edit]`。能力名は実行環境に合わせて選ぶ。 |

検証の単発Taskなら `type: verification` とし、本文に検証内容と終了条件を書く。Taskの存続形態を表す `standing / temporary / focused / conditional` は `type` に使わない。`status: conditional` は現在の選択条件を表すため引き続き使用する。

## 作業を完了と判断する

Taskが意図した問いに、必要な根拠・行為・品質確認を通じて答えたかを見る。時間、操作数、文章量、ファイル数は完了の代わりにならない。

複雑なTaskでは、「問いの設定 → 根拠確認 → 分析・実行 → 品質確認 → Wiki更新 → 必要な引継ぎ」を目安にする。計画が目的なら計画の完成が成果であり、実行が目的なら実行前の計画だけでは完了しない。点検で変更不要と分かった場合は、確認範囲と根拠を記録すればよく、不要な改稿を作らない。

観察、推論、実行した検証、クライアント確認を区別する。表記と意味は [検証状況](wiki/verification.md) を参照する。`executed` は実行した事実を示し、成功の判定は別に記述する。

## 検証Taskと結果の記録

検証実験は `type: verification` のTaskとして作成する。実行前に、対象の主張、比較基準、操作条件、判定基準、必要な環境・権限、終了・停止条件、Wikiの記録先を本文へ書く。コマンドが未確定なら、実行時に特定してRun Noteへ残すことを明記する。コードを変更する場合は許可された変更範囲も記す。

結果、重要な測定値、成功・失敗・判定不能、適用条件、対象の版、設計への影響は、[検証状況](wiki/verification.md) または関連するWiki文書へまとめる。Run Noteを開かなくても、その結果が何を意味するか分かるようにする。

Run Noteには実行コマンド、環境、対象の版、未コミット変更の有無と内容をたどれる差分等の参照、ログ、生成物への参照、試行の経緯を残す。ログに含まれる出力値は証拠として保持し、判断に必要な値と意味はWikiにも整理する。作業過程の振り返りから再利用価値の高い手順・注意点が得られた場合はWikiへまとめる。

## 次に行うTaskを選ぶ

初期版は手動または対話で選ぶ。実施回数の均等化より、事業への影響、期限、不確実性、調べることで得られる知識、最後に確認した時期、未点検の範囲を見る。

- `P0`：セキュリティ、データ損失、重大障害、即時の事業継続リスク。
- `P1`：クライアント対応やリリースを直接妨げている判断・検証。
- `P2`：重要な設計、要求との整合、未検証事項。
- `P3`：継続改善、選択肢の探索。
- `P4`：Task運用自体の点検。具体的な問題が繰り返された場合に実施する。

### 自動割当が必要になった場合

多数のTaskを複数の実行者へ自動配分する必要が生じたら、`registry.json` の導入を検討できる。導入時は `status / priority / due / trigger / claim` などの配分情報をそこへ移し、Task冒頭から重複する情報を削除する。`due` は期限、`claim` は担当の確保を表す。検査スクリプトも新しい構成に合わせて変更する。

## 実行者と実行環境を選ぶ

実行者は利用可能な能力で選ぶ。

- **資料読解ができる環境**：コード・履歴・Issue・PR・外部資料の読解、比較、レビュー、設計整理、改稿を行う。シェルやテストを実行できなければ、必要な検証をTaskとして引き継ぐ。
- **ローカル実行ができる環境**：型検査、lint、テスト、測定、性能分析、不具合再現、試作、許可されたコード変更を行う。実行ログはRun Note、結果と判断はWikiへ記録する。
- **人間**：要求の意味、契約・費用・納期、不可逆な設計変更、業務上の許容リスクなど、人間の判断が必要な事項を確認する。

典型的には、資料読解で問いを絞り、検証Taskを実行環境へ渡し、結果をWikiにまとめ、Run Noteから詳細を追えるようにする。

## 権限と取り扱いの範囲

Taskの重要性と実行権限は別である。

- 秘密鍵、トークン、認証情報をWikiやRun Noteへ転記しない。
- 顧客・個人データは必要最小限にし、利用を許可された手段で扱う。
- 本番変更、外部送信、課金を伴う操作、不可逆変更は、明示された権限と停止条件の範囲で行う。
- 権限が足りなければ停止理由と再開条件を記録し、無断で代替操作を行わない。

## Task運用の点検

[Task運用の点検](tasks/task-system-health.md) は、同じTaskへの偏り、引継ぎの滞留、Wikiへの結果の反映漏れ、作業候補の放置、文書の肥大化などが繰り返された場合に実施する。

一度の改善では、候補の選び方、Taskの書き方、作業の割当、情報の整理方法のいずれかに対象を絞る。観察だけで十分なら変更しない。ルールや記録の件数ではなく、理解・判断・検証・文章保守が改善したかを見る。

運用ルールはこの文書にまとめる。特定部分の更新周期・担当者・機械処理が継続的に異なるようになった場合に、分割を検討する。
