# Taskの運用

Taskは、設計理解、要求との整合、未検証事項、選択肢の比較、コードと文書のずれを継続的に扱う作業である。運用ルールの整備は、それらの作業を進めやすくするために行う。具体的な問題がなければ主要な開発・設計・検証を優先する。

標準の一回の起動は **2 Work + E** とする。二つのWorkは独立した実作業、Slot Eは `WIKI-EDITORIAL-QUALITY` を実行する独立した品質管理の枠である。Work側の局所集中を抑えることと、今回の作業対象から外れたWiki領域の品質を保つことを別々に担う。

## TODOを人間向けの要望窓口として扱う

[`TODO.md`](TODO.md) は、人間が明示的な要望を書き、現在状態を確認するための入口である。具体的な機能追加、修正、調査、優先度変更、文書改善等を、観察・感想・一般知識とは分けて追跡する。

作業方法を決める前にREADMEとTODOをその都度読み直す。新しい人間要望を見つけたら、まずTODOへ記録または整形し、その要求を既存Task内で扱うか、新しいTaskへ形成するか、直接作業で閉じるかを判断する。TODOに書かれたことを自動的に独立Taskへ変換しない。

一回の起動を閉じる前にもTODOを再読する。今回の成果で人間向けの要求状態が変わった場合は、`open / planned / in-progress / blocked` 等の補足、実行先、完了条件、チェック状態を更新する。Taskを一回実行したことと、TODOの要求が満たされたことを同一視しない。

観察と要求が混在する場合は両方を保存できる。たとえば「この画面は使いにくいので並び替えを追加してほしい」は、使いにくさの観察をWikiやRunへ、追加要望をTODOへ分けて扱える。要求の追跡と知識形成を片方へ還元しない。

## 標準起動 — 2 Work + E

### Work 1

人間からの直接依頼があればWork 1に固定する。そうでなければTODOと選択可能なTaskから、事業影響、期限、不確実性、最後の確認時期、具体的な次の問いを踏まえて選ぶ。

Work 1は対象Taskをその都度読み直し、一回の作業の完了条件まで進める。Work 2やSlot Eへ未完了を逃がさない。

### Work 2

Work 2はWork 1とは独立に選ぶ。Work 1の直前成果だけから次の問いを作らず、TODOとTask一覧をもう一度確認し、最近実行されていない重要領域、異なるClient Need、異なる証拠種別、異なる設計面を候補にする。

同じTaskをWork 1とWork 2に選ぶこと自体は禁止しない。ただし各枠が、少なくとも `判別可能な問い / 主な根拠面 / 判断差 / 停止条件` を別々に持つことを確認する。同じ証拠を二つの見出しへ分割しただけ、またはWork 1がWork 2の対象・問いを直接生成して直列工程になった場合は、一つのWork内の下位工程として扱う。

### Slot E

Slot Eでは [Wiki内部品質の独立点検](tasks/wiki-editorial-quality.md) を実行する。対象は原則としてWork 1 / Work 2で新設・実質改稿した文書、両Workが中心資料として精読した文書、同じ判断系列へ直接属する文書を避け、現在の作業主題から十分に離れた既存Wiki Entryから選ぶ。

対象候補では、長く再読されていない文書、参照されやすい文書、肥大化した文書、要求・実装の変更に記述が追いついていない可能性がある文書を優先する。Wikiがまだ小さく完全に無関係な文書を選べない場合は、今回変更していない文書のうち最も関係が薄いものを選び、その制約をRun Noteへ残す。

文章品質は各Work自身の品質基準でもある。Eは「Workでは粗く書いてよい」という免責でもWork成果の再校正の枠でもない。Eは別のWiki Entryを通読し、中心claim、根拠、適用範囲、反例、用語、文章構造、リンク、正本境界を診断して、必要なら改稿する。変更不要という判断も成立する。

Eで新しいドメイン調査、大規模な設計再判断、実行検証が必要な欠陥を発見した場合は、憶測で埋めず後続Taskとしての実施方法を検討する。Slot Eの目的は独立品質管理であり、別の実作業を無制限に開始することではない。

### 例外

P0相当の障害、安全上の緊急対応、明確な権限・実行時間不足ではWork 1を優先して停止できる。Run Noteには2Slot+Eを閉じられなかった理由と再開条件を記す。通常の忙しさや「Work 1が十分大きかった」だけを省略理由にしない。

## 二枠制で見る配分バランス

二つのWorkは単純な回数均等化のために置かない。自由選択では、毎回新しい問いを作りやすいTask、直前成果から再開しやすいTask、開始コストが低いTaskへ局所的に集中しうるため、Work 2で別候補を独立に見直す。

選択時には、`priority / 最終露出 / 選択可能な具体的問い / 期待情報利得 / 開始コスト / 直前文脈からの再開しやすさ / TODO上の人間優先度` を区別する。短期集中が高い情報利得を持つ場合もあるため、集中そのものを失敗にしない。具体的で高価値な問いがあるのに長く実行されない状態が続く場合に、Task運用上の偏りとして扱う。

## 作業候補をTaskにする

作業中に問題や改善機会を見つけたら、必要に応じて [作業候補](tasks/proposals.md) へ記録する。人間の明示的要求ならTODOにも現在状態を残す。目的、必要期間、繰り返す理由、引継ぎの必要性、終了条件から、次の扱いを選ぶ。

- **既存Taskの一部として行う**：目的・品質基準・停止条件が既存Taskと同じ。
- **単発のTaskにする**：一回または少数回で終わるが、後日または別の実行者へ引き継ぐ必要がある。
- **完了まで集中的に進めるTaskにする**：複数回の作業が必要で、明確な達成条件がある。
- **条件付きで実行するTaskにする**：障害再発、閾値超過、特定の移行などをきっかけに実施する。
- **常設のTaskにする**：独立した責任があり、繰り返すことで新しい価値が生まれる。
- **Taskにしない**：既存Taskで十分、根拠が弱い、費用に見合わない、まだ具体的な問いがない。

これらは存続形態や実施方法であり、`type` の値には使わない。個別Taskの本文に期間・頻度・終了条件として記す。

「要望や問題の兆候 → TODOまたは作業候補 → 形成先の判断 → 必要なら試行 → 実施・引継ぎ → 終了・再評価」を目安にする。候補数やTask数の増加を成果とみなさない。

## Taskに書く内容

Taskは、何を根拠に何を行い、どこまで進めればよいかを定める。[テンプレート](tasks/_TEMPLATE.md) を使い、複雑さとリスクに応じた詳しさで書く。

- **目的・判断対象**：何を理解、判断、検証、保守するか。
- **一次資料・証拠**：コード、Git履歴、要求、ログ、テスト、外部仕様等。
- **必要な行為**：読解、比較、静的解析、実行、測定、改稿等。
- **一回の作業の完了条件**：今回の範囲について、どの確認と記録まで終えるか。
- **品質基準**：根拠との一致、反例の検討、実行確認、文章の読み直し等。
- **存続形態・終了・停止・引継ぎ**：繰り返す理由、Task自体の終了条件、作業を止める理由、再開に必要な条件。
- **記録先**：結果・判断を書くWikiと、実行経緯を書くRun Note。人間要望に由来する場合はTODOとの関係も示す。

一回の作業の完了とTask自体の終了は分けて考える。具体的な終了・中断・再開の判断は個別Taskの本文に定め、共通の状態遷移は設けない。

## メタデータ

各Taskは、次の項目を空欄にせず記入する。

| 項目 | 意味 |
|---|---|
| `id` | 文書を参照するための重複しないID。既存の標準TaskのIDは維持する。 |
| `type` | 作業内容。標準値は `maintenance`、`review`、`exploration`、`verification`、`editorial`、`work`。必要なら内容を説明できる名称を追加する。 |
| `status` | 現在の扱い。標準Taskは `active`、条件成立時だけ選択するものは `conditional`、導入時に使わないものは `paused`。 |
| `priority` | 下記の `P0`〜`P4`。 |
| `trigger` | 実施を検討するきっかけ。随時なら `on-demand`。 |
| `required_capabilities` | 必要能力の空でないリスト。例：`[code-read, doc-edit]`。 |

検証の単発Taskなら `type: verification` とし、本文に検証内容と終了条件を書く。Taskの存続形態を表す `standing / temporary / focused / conditional` は `type` に使わない。

### Run Noteの `task` 項目

Run Noteの冒頭のメタデータは `run_id / task / executor` を必須とする。`task` は一行の文字列のまま、カンマ区切りで複数Taskを列挙できる。

標準2Slot+Eでは **Work 1, Work 2, Slot Eの順** に記録する。たとえば `task: direct-request, DESIGN-MAINTENANCE, WIKI-EDITORIAL-QUALITY` とする。同じTaskを二つのWorkで独立した作業として選んだ場合は `task: DESIGN-MAINTENANCE, DESIGN-MAINTENANCE, WIKI-EDITORIAL-QUALITY` のような重複も許容する。

`direct-request` はTask Entryを持たない直接依頼を表す特別値である。緊急停止等で2Slot+Eの一部だけを実行したRunは、実際に実行したTaskだけを順に列挙し、本文で省略理由を記録する。空要素、存在しないTask ID、角括弧を使う配列構文は使わない。

## 作業を完了と判断する

Taskが意図した問いに、必要な根拠・行為・品質確認を通じて答えたかを見る。時間、操作数、文章量、ファイル数は完了の代わりにならない。

複雑なTaskでは、「問いの設定 → 根拠確認 → 分析・実行 → 品質確認 → Wiki更新 → 必要な引継ぎ」を目安にする。計画が目的なら計画の完成が成果であり、実行が目的なら実行前の計画だけでは完了しない。点検で変更不要と分かった場合は、確認範囲と根拠を記録すればよく、不要な改稿を作らない。

観察、推論、実行した検証、クライアント確認を区別する。表記と意味は [検証状況](wiki/verification.md) を参照する。`executed` は実行した事実を示し、成功の判定は別に記述する。

## Wiki Entry-level claim と Run-level contribution claim

claimは二つのレベルを区別する。

### Wiki Entry-level claim

Wiki Entry-level claimは、後続者が再利用・支持・懐疑・改訂できる**現在の再利用可能な命題**である。設計判断なら現在何を選ぶべきか、実践知・調査知識なら現在何が成り立つかを、根拠、適用範囲、反例、未検証事項とともに記す。

一つの文書に、異なる根拠や反証条件を持つ複数の中心命題がある場合は `Claim 1 / Claim 2` のように必要な範囲で分ける。文を形式論理の原子命題へ分解する必要はなく、後続者が一方だけを支持・懐疑できる粒度を目安にする。

### Run-level contribution claim

Run-level contribution claimは、一つの独立Task実行が既存状態へ与えた**知的差分**である。「調べた」「ファイルを更新した」ではなく、何を新しく確認し、どの理解を追加・限定・反証・実行確認したかを書く。

標準Run NoteではWork 1、Work 2、Slot Eにそれぞれ貢献主張を置く。二つのWorkが実際に同じ上位原理へ独立に収束した場合だけ横断主張を追加できる。単一の横断物語を必須にしない。

Run-level claimはWikiの中心主張を丸写ししない。たとえばWikiが「A方式は条件Xで採用できる」と主張し、今回のRunが負荷試験で条件Xの上限を狭めたなら、Run claimは「負荷試験によりA方式の適用上限をYまで限定した」と書き、Wikiの現在主張へ反映する。Eでは「どの理解・表現・境界を明確化、限定、修復したか」を記す。

### 既存claimへの反応

既存Wikiの特定claimを実質的に利用・変更した場合は、Run Noteで対象命題を特定して `SUPPORT` または `CHALLENGE` を記録できる。ファイルを参照しただけで中心claimへの支持とみなさず、異なる対象・根拠・反証条件を一つの姿勢項目へ束ねない。

SUPPORT / CHALLENGEを記録する場合は、対象命題の短い引用、文書内の見出しまたはClaim番号、参照時のコミットSHAを残す。可能ならそのコミットに固定した文書へのリンクも付ける。改稿後も、どの版のどの命題を評価したかを復元できるようにする。

未コミットの文書を評価した場合は、基準コミットSHAに加え、評価時の差分または文書の保存先を記録する。Git管理外の資料では版・取得日と保存した資料への参照を残す。現在版へのリンクだけで、当時の命題を特定したことにしない。

改稿は姿勢そのものではない。旧claimのどこを退け、新しいclaimのどこを支持するかを区別できる場合に、改訂としてWikiへ反映する。

### 評判を点数化しない

後続者による再利用、SUPPORT、CHALLENGE、改訂は、claimがどのように作用したかを知る証拠になる。一方、再利用されたことは妥当性の証明ではなく、大きな改稿を誘発したことも元claimの正しさを意味しない。

同じRun内で自分が自分のclaimを支持しても後続評判の証拠にはしない。参加者順位、評判点数、支持件数最大化は導入しない。妥当性は一次証拠、反証、独立検証等から評価し、再利用や作用深度とは別軸で扱う。

## 検証Taskと結果の記録

検証実験は `type: verification` のTaskとして作成する。実行前に、対象のclaim、比較基準、操作条件、判定基準、必要な環境・権限、終了・停止条件、Wikiの記録先を本文へ書く。コマンドが未確定なら、実行時に特定してRun Noteへ残すことを明記する。コードを変更する場合は許可された変更範囲も記す。

結果、重要な測定値、成功・失敗・判定不能、適用条件、対象の版、設計への影響は、[検証状況](wiki/verification.md) または関連するWiki文書へまとめる。Run Noteを開かなくても、その結果が何を意味するか分かるようにする。

Run Noteには実行コマンド、環境、対象の版、未コミット変更の有無と内容をたどれる差分等の参照、ログ、生成物への参照、試行の経緯を残す。作業過程から再利用価値の高い手順・注意点が得られた場合はWikiへまとめる。

## 次に行うTaskを選ぶ

初期版は手動または対話で選ぶ。Work 1 / Work 2とも、実施回数の均等化より、事業への影響、期限、不確実性、調べることで得られる知識、最後に確認した時期、未点検の範囲を見る。Slot Eは通常この候補群から選ばず、`WIKI-EDITORIAL-QUALITY` に固定する。

- `P0`：セキュリティ、データ損失、重大障害、即時の事業継続リスク。
- `P1`：クライアント対応やリリースを直接妨げている判断・検証。
- `P2`：重要な設計、要求との整合、未検証事項。
- `P3`：継続改善、選択肢の探索。
- `P4`：Task運用自体の点検。具体的な問題が繰り返された場合に実施する。

### 自動割当が必要になった場合

多数のTaskを複数の実行者へ自動配分する必要が生じたら、`registry.json` の導入を検討できる。導入時は `status / priority / due / trigger / claim` などの配分情報をそこへ移し、Task冒頭から重複する情報を削除する。検査スクリプトも新しい構成に合わせて変更する。

## 実行者と実行環境を選ぶ

実行者は利用可能な能力で選ぶ。

- **資料読解ができる環境**：コード・履歴・Issue・PR・外部資料の読解、比較、レビュー、設計整理、改稿を行う。シェルやテストを実行できなければ、必要な検証をTaskとして引き継ぐ。
- **ローカル実行ができる環境**：型検査、lint、テスト、測定、性能分析、不具合再現、試作、許可されたコード変更を行う。実行ログはRun Note、結果と判断はWikiへ記録する。
- **人間**：要求の意味、契約・費用・納期、不可逆な設計変更、業務上の許容リスクなど、人間の判断が必要な事項を確認する。

典型的には、資料読解で問いを絞り、検証Taskを実行環境へ渡し、結果をWikiにまとめ、Run Noteから詳細を追えるようにする。

## コミットメッセージ

ユーザーの直接指示によらない通常のLLM Wiki更新をコミットする場合は、コミットメッセージの先頭に必ず `wiki:` を付ける。例：`wiki: 設計判断の根拠を更新`。ユーザーの直接指示に基づく更新には、この接頭辞を必須としない。

この規則は、自律的に選んだTaskやSlot Eに伴うWiki・Task・Run Note等の通常更新に適用する。判断は起動全体ではなく、そのコミットに含める更新が直接指示に基づくかどうかで行う。

## 権限と取り扱いの範囲

Taskの重要性と実行権限は別である。

- 秘密鍵、トークン、認証情報をWikiやRun Noteへ転記しない。
- 顧客・個人データは必要最小限にし、利用を許可された手段で扱う。
- 本番変更、外部送信、課金を伴う操作、不可逆変更は、明示された権限と停止条件の範囲で行う。
- 権限が足りなければ停止理由と再開条件を記録し、無断で代替操作を行わない。

## Task運用の点検

[Task運用の点検](tasks/task-system-health.md) は、同じTaskへの偏り、Work 1からWork 2への直列化、Editorial対象の局所集中、引継ぎの滞留、Wikiへの結果の反映漏れ、TODOや作業候補の放置、文書の肥大化などが繰り返された場合に実施する。

一度の改善では、TODOからの形成、Taskの書き方、2Slot選択、Editorial対象選択、情報の整理方法のいずれかに対象を絞る。観察だけで十分なら変更しない。ルールや記録の件数ではなく、理解・判断・検証・Wiki内部品質・文章保守が改善したかを見る。

運用ルールはこの文書にまとめる。特定部分の更新周期・担当者・機械処理が継続的に異なるようになった場合に、分割を検討する。
